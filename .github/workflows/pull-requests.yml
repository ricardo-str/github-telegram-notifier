name: 'Telegram Notifications - Pull Requests'

# Temporalmente deshabilitado para desarrollo
# Habilitar cuando se use en el repositorio de producción
on:
  workflow_dispatch:  # Solo ejecutar manualmente
  # pull_request:
  #   types: [opened, closed, ready_for_review, review_requested]

jobs:
  notify-telegram:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Send Telegram notification
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: node src/telegram-notifier.js
