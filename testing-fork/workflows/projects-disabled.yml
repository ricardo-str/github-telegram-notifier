# TEMPORALMENTE DESHABILITADO
# El evento projects_v2_item requiere permisos de organización
# que no están disponibles en repositorios personales

# Cuando se instale en paisaje-biblio (organización), 
# renombrar este archivo a projects.yml para habilitarlo

name: 'Telegram Notifications - Projects (DISABLED)'

# on:
#   projects_v2_item:
#     types: [created, edited, deleted, converted, reordered]

on:
  workflow_dispatch:  # Solo manual

jobs:
  notify-telegram:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "Este workflow está deshabilitado para testing en repo personal"
